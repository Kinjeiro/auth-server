// todo @ANKU @LOW - тесты для users
// describe('[api] users', () => {
  // describe('[route] /api/users/:id/remove', () => {
  //   it('should be valid response', async () => {
  //     const {
  //       status,
  //       text,
  //     } = await chai.request(server)
  //       .get('/api/health');
  //
  //     expect(status).to.equal(200);
  //     expect(text).to.equal('OK');
  //   });
  // });
// });
